# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: auth_group
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: name
      external_name: name
      # Type: String Stats: 1 properties of this type in this kind.

    - property: permissions
      external_name: permissions
      # Type: String Stats: 24 properties of this type in this kind.


- kind: auth_user
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: date_joined
      external_name: date_joined
      # Type: Date/Time Stats: 11 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: email
      external_name: email
      # Type: Email Stats: 4 properties of this type in this kind.
      import_transform: db.Email

    - property: first_name
      external_name: first_name
      # Type: String Stats: 7 properties of this type in this kind.

    - property: groups
      external_name: groups
      # Type: Key Stats: 11 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: is_active
      external_name: is_active
      # Type: Boolean Stats: 11 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: is_staff
      external_name: is_staff
      # Type: Boolean Stats: 11 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: is_superuser
      external_name: is_superuser
      # Type: Boolean Stats: 11 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: last_login
      external_name: last_login
      # Type: Date/Time Stats: 11 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: last_name
      external_name: last_name
      # Type: String Stats: 7 properties of this type in this kind.

    - property: password
      external_name: password
      # Type: String Stats: 11 properties of this type in this kind.

    - property: username
      external_name: username
      # Type: String Stats: 11 properties of this type in this kind.


- kind: django_admin_log
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: action_flag
      external_name: action_flag
      # Type: Integer Stats: 5397 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: action_time
      external_name: action_time
      # Type: Date/Time Stats: 5397 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: change_message
      external_name: change_message
      # Type: Text Stats: 5397 properties of this type in this kind.
      import_transform: db.Text

    - property: content_type
      external_name: content_type
      # Type: String Stats: 5397 properties of this type in this kind.

    - property: object_id
      external_name: object_id
      # Type: String Stats: 5397 properties of this type in this kind.

    - property: object_repr
      external_name: object_repr
      # Type: Text Stats: 5397 properties of this type in this kind.
      import_transform: db.Text

    - property: user
      external_name: user
      # Type: Key Stats: 5397 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string


- kind: django_session
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: data
      external_name: data
      # Type: Blob Stats: 841 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: expiry
      external_name: expiry
      # Type: Date/Time Stats: 841 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')


- kind: django_site
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: domain
      external_name: domain
      # Type: String Stats: 6 properties of this type in this kind.

    - property: name
      external_name: name
      # Type: String Stats: 6 properties of this type in this kind.


- kind: ladypenh_article
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: content
      external_name: content
      # Type: Text Stats: 5 properties of this type in this kind.
      import_transform: db.Text

    - property: date
      external_name: date
      # Type: Date/Time Stats: 143 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: header
      external_name: header
      # Type: Text Stats: 143 properties of this type in this kind.
      import_transform: db.Text

    - property: legacy_comment_url
      external_name: legacy_comment_url
      # Type: String Stats: 15 properties of this type in this kind.

    - property: numid
      external_name: numid
      # Type: Integer Stats: 143 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: piccredits
      external_name: piccredits
      # Type: String Stats: 143 properties of this type in this kind.

    - property: picname
      external_name: picname
      # Type: String Stats: 143 properties of this type in this kind.

    - property: tags
      external_name: tags
      # Type: Key Stats: 256 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: title
      external_name: title
      # Type: String Stats: 143 properties of this type in this kind.


- kind: ladypenh_event
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: date
      external_name: date
      # Type: Date/Time Stats: 2033 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: description
      external_name: description
      # Type: Text Stats: 2033 properties of this type in this kind.
      import_transform: db.Text

    - property: haslargepic
      external_name: haslargepic
      # Type: Boolean Stats: 2033 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: highlight
      external_name: highlight
      # Type: Boolean Stats: 2033 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: numid
      external_name: numid
      # Type: Integer Stats: 2033 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: organizer
      external_name: organizer
      # Type: String Stats: 203 properties of this type in this kind.

    - property: picheight
      external_name: picheight
      # Type: Integer Stats: 1832 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: picname
      external_name: picname
      # Type: String Stats: 1832 properties of this type in this kind.

    - property: picwidth
      external_name: picwidth
      # Type: Integer Stats: 1832 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: shortdesc
      external_name: shortdesc
      # Type: Text Stats: 2033 properties of this type in this kind.
      import_transform: db.Text

    - property: status
      external_name: status
      # Type: String Stats: 2033 properties of this type in this kind.

    - property: time
      external_name: time
      # Type: Date/Time Stats: 2033 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: title
      external_name: title
      # Type: String Stats: 2033 properties of this type in this kind.

    - property: type
      external_name: type
      # Type: String Stats: 2033 properties of this type in this kind.

    - property: until
      external_name: until
      # Type: String Stats: 111 properties of this type in this kind.

    - property: venue
      external_name: venue
      # Type: Key Stats: 2033 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string


- kind: ladypenh_friend
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: desc
      external_name: desc
      # Type: Text Stats: 23 properties of this type in this kind.
      import_transform: db.Text

    - property: type
      external_name: type
      # Type: String Stats: 23 properties of this type in this kind.


- kind: ladypenh_imagefile
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: blob
      external_name: blob
      # Type: Blob Stats: 2446 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: name
      external_name: name
      # Type: String Stats: 2446 properties of this type in this kind.


- kind: ladypenh_oneliner
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: daycode
      external_name: daycode
      # Type: Integer Stats: 1 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: dayend
      external_name: dayend
      # Type: Date/Time Stats: 86 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: dayspan
      external_name: dayspan
      # Type: Date/Time Stats: 2 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: daystart
      external_name: daystart
      # Type: Date/Time Stats: 86 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: friday
      external_name: friday
      # Type: Boolean Stats: 86 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: monday
      external_name: monday
      # Type: Boolean Stats: 86 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: saturday
      external_name: saturday
      # Type: Boolean Stats: 86 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: sunday
      external_name: sunday
      # Type: Boolean Stats: 86 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: thursday
      external_name: thursday
      # Type: Boolean Stats: 86 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: time
      external_name: time
      # Type: Date/Time Stats: 2 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: title
      external_name: title
      # Type: String Stats: 86 properties of this type in this kind.

    - property: tuesday
      external_name: tuesday
      # Type: Boolean Stats: 86 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: wednesday
      external_name: wednesday
      # Type: Boolean Stats: 86 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)


- kind: ladypenh_tag
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: name
      external_name: name
      # Type: String Stats: 16 properties of this type in this kind.


- kind: ladypenh_venue
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: address
      external_name: address
      # Type: String Stats: 154 properties of this type in this kind.

    - property: details
      external_name: details
      # Type: Text Stats: 3 properties of this type in this kind.
      import_transform: db.Text

    - property: ladypenh_url
      external_name: ladypenh_url
      # Type: String Stats: 1 properties of this type in this kind.

    - property: linktext
      external_name: linktext
      # Type: String Stats: 3 properties of this type in this kind.

    - property: linkurl
      external_name: linkurl
      # Type: String Stats: 4 properties of this type in this kind.

    - property: mapurl
      external_name: mapurl
      # Type: String Stats: 36 properties of this type in this kind.

    - property: name
      external_name: name
      # Type: String Stats: 154 properties of this type in this kind.

    - property: oneshot
      external_name: oneshot
      # Type: Boolean Stats: 154 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)


- kind: ladypenh_venuefile
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: blob
      external_name: blob
      # Type: Blob Stats: 35 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: filename
      external_name: filename
      # Type: String Stats: 35 properties of this type in this kind.

    - property: name
      external_name: name
      # Type: String Stats: 35 properties of this type in this kind.

    - property: valid_until
      external_name: valid_until
      # Type: Date/Time Stats: 35 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: venue
      external_name: venue
      # Type: Key Stats: 35 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

